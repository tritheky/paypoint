const database = require('../database');

module.exports = {
  newUsers,
  rewardResult,
  rewardDone,
};

async function newUsers(bookingIds) {
  for (const bookingId of bookingIds) {
  }
  return true;
}
async function rewardResult(bookingIds) {
  for (const bookingId of bookingIds) {
  }
  return true;
}
async function rewardDone(bookingIds) {
  for (const bookingId of bookingIds) {
  }
  return true;
}
